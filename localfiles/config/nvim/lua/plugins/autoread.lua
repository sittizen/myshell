return {
    "manuuurino/autoread.nvim",
    cmd = "Autoread",
    opts = {},
    config = {
        interval = 500, -- in milliseconds
        notify_on_change = true,
        cursor_behavior = "preserve",
    },
}
